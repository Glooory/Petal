<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/coordinator"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

        <ImageView
            android:id="@+id/imageview_splash"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fitsSystemWindows="true"
            android:scaleType="centerCrop"
            android:src="@drawable/bg_splash_screen_bulb"/>

        <ImageView
            android:id="@+id/imageview_splash_logo"
            android:layout_width="144dp"
            android:layout_height="wrap_content"
            android:layout_alignParentStart="true"
            android:layout_alignParentTop="true"
            android:layout_marginStart="@dimen/splash_logo_margin_start"
            android:layout_marginTop="@dimen/splash_logo_margin_top"
            android:adjustViewBounds="true"
            android:alpha="0"
            android:fitsSystemWindows="true"
            android:src="@drawable/ic_huaban_logo_red_200"/>

</android.support.design.widget.CoordinatorLayout>